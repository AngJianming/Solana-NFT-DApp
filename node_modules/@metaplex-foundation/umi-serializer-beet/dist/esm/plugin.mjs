import { createBeetSerializer } from './createBeetSerializer.mjs';

const beetSerializer = (options = {}) => ({
  install(umi) {
    umi.serializer = createBeetSerializer(options);
  }
});

export { beetSerializer };
//# sourceMappingURL=plugin.mjs.map
